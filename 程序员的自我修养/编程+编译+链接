c和c++的访问权限都是以文件划分的，故编译获得的.o文件中包含源文件中的所有变量和函数的符号表，如果有头文件，那么头文件会被复制黏贴到c文件中，然后接受编译

现在有1.c 2.c，在2.c中有全局变量int a_2 = 10，那么对于2.c的编译将会把a_2放入2.o的符号表中，如果1.c不使用extern表示“自己想使用其他文件中的变量a_2”，那么
对于1.c的编译无法通过。

链接是把各个.o文件的符号表相互连上，如果1.c和2.c中都有 int i，那么链接时会发现重复，导致链接失败`
